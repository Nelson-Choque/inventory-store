openapi: 3.1.0
info:
  title: API de Productos
  description: Documentación de la API de Productos
  version: 1.0.0
components:
  schemas:
    Producto:
      $ref: "./schemas/producto.yaml"
    Persona:
      $ref: "./schemas/persona.yaml"

paths:
  /producto/:
    $ref: "./paths/producto.yaml"
  /producto/{id}:
    $ref: "./paths/productoID.yaml"
  /persona/:
    $ref: "./paths/persona.yaml"
  /persona/{id}:
    $ref: "./paths/personaID.yaml"
